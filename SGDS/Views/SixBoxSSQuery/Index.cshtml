<script src="~/Scripts/gis/ext/meter/BasePinCtrl.js"></script>
<script src="~/Scripts/prj/data.js"></script>
<script src="~/Scripts/prj/sixboxss.js"></script>
<style>
    .modal-sixboxss {
        overflow: hidden;
        height: 100%;
    }

        .modal-sixboxss .modal-dialog {
            max-width: 100% !important;
            height: 100%;
        }

    .sixboxss {
        margin: 0 !important;
        padding: .5rem !important;
        /* width: 100% !important;
        height: 100% !important;
        display: inline !important;*/
    }

        .sixboxss .modal-content {
            width: 100% !important;
            height: 100% !important;
            overflow: auto;
        }

    /* toggle container */
    .time-type-ctrl-container{
        display:flex;
        padding-top:.25em !important;
        margin-right:.5rem;
    }
    .time-type-ctrl {
        margin-right: 0 !important;
        /*width: 3em !important;
        height: 1.25em;*/
        width: 5em !important;
        height: 1.75em;
        margin-top: 0.125em;
        cursor: pointer;
    }
    .time-type-ctrl:before{
        content:"依時間";
        margin-left:1.5em;
    }
    .time-type-ctrl:checked:before {
        display:none;
    }
    .time-type-ctrl:after{
        content:"依事件";
        margin-left:0.25em;
    }
     .time-type-ctrl:not(:checked):after {
        display:none;
    }
    .time-type-ctrl:not(:checked), .time-type-ctrl:not(:checked) ~ .time-type-ctrl::before {
        background-color: lightblue; /* Change the background color of the toggle container  when it is not checked */
        border-color: blue; /* Change the border color of the toggle container when it is not checked */
        border: 2px solid purple; /* Add a border if you prefer */
    }
     
    .time-type-ctrl:checked,.time-type-ctrl:checked ~ .time-type-ctrl::before {
        background-color: pink; /* Change the color of the toggle container when it is checked */
        border-color: red; /* Change the border color of the toggle container when it is checked */
        border: 2px solid violet; /* Add a border if you prefer */
    }
    .event-ctrl-container select{
        min-width:238px;
    }
    .event-ctrl-container select:first-child {
        min-width: 170px;
    }
        /* toggle knob */
        /*.card-checkbox.checkbox-outside-card.custom-control-input:not(:checked) ~ .custom-control-label::after {
        background-color: #fff;*/ /* Change the background color of the knob when it is checked */
        /*border: 2px solid purple;*/ /* Add a border if you prefer */
        /*}

    .card-checkbox.checkbox-outside-card.custom-control-input:checked ~ .custom-control-label::after {
        background-color: pink;*/ /* Change the background color of the knob when it is checked */
        /*border: 2px solid violet;*/ /* Add a border if you prefer */
        /*}*/
</style>
@using Dou.Misc.Extension
@Html.MenuMap()
@Html.DataManagerOptionsJavascript(null, "douoptions")

<table id="_table"> </table>
